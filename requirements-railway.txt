# TORQ Console - Railway Production Dependencies
# Full dependencies for complete WebUI functionality

# Core web framework
click>=8.0.0
rich>=13.0.0
prompt_toolkit>=3.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
websockets>=11.0.0
python-socketio>=5.9.0
aiofiles>=23.0.0
aiohttp>=3.8.0
httpx>=0.24.0
pydantic>=2.8.0,<3.0.0
python-multipart>=0.0.6
jinja2>=3.1.0

# AI API clients (cloud-based, no local models needed)
openai>=1.0.0
anthropic>=0.20.0

# Git integration (for file operations and git commands)
gitpython>=3.1.0

# Process monitoring and utilities
psutil>=5.9.0
watchdog>=3.0.0
typer>=0.9.0

# JSON-RPC for MCP communication
jsonrpcserver>=5.0.0
jsonrpcclient>=4.0.0

# Configuration
PyYAML>=6.0.0

# Supabase (learning events persistence)
supabase>=2.3,<3
httpx>=0.24.0

# Optional but helpful
python-dotenv>=1.0.0
