# TORQ Console - Vercel Deployment Requirements
# Minimal dependencies for Enhanced Prince API on Vercel

# Core web framework (required for FastAPI)
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.8.0,<3.0.0
python-multipart>=0.0.6

# AI API clients (cloud-based, no local models)
anthropic>=0.20.0
openai>=1.0.0

# HTTP clients and async support
httpx>=0.24.0
aiohttp>=3.8.0
aiofiles>=23.0.0

# Core utilities
click>=8.0.0
rich>=13.0.0
PyYAML>=6.0.0

# JSON-RPC for potential MCP communication
jsonrpcserver>=5.0.0
jsonrpcclient>=4.0.0

# Git integration (lightweight)
gitpython>=3.1.0

# Configuration management
python-dotenv>=1.0.0

# Process utilities (minimal)
psutil>=5.9.0
