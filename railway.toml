[deploy]
# Railway will use Dockerfile (auto-detected)
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Simple healthcheck endpoint that returns immediately
healthcheckPath = "/health"
healthcheckTimeout = 300
