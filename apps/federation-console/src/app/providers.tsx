/**
 * TORQ Federation Console - App Providers
 * Phase 1B - Provider wrapper components
 */

import type { PropsWithChildren } from 'react'

export function AppProviders({ children }: PropsWithChildren) {
  return <>{children}</>
}
