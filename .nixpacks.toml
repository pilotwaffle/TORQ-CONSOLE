[phases.setup]
nixPkgs = ["python311", "pip", "gcc", "gnumake"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install --no-cache-dir -r requirements-railway.txt"
]

[phases.build]
cmds = []

[start]
cmd = "uvicorn torq_console.ui.railway_app:app --host 0.0.0.0 --port $PORT"
