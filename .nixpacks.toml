[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install --no-cache-dir -r requirements-railway.txt"
]

[phases.build]
cmds = []

[start]
cmd = "python -m torq_console.ui.railway_main --host 0.0.0.0 --port $PORT"
